---
- name: Install core tools
  hosts: swarm_cluster
  tasks:
    - name: Install pip3
      become: yes
      apt:
        update_cache: true
        name: python3-pip
    - name: Install Python docker module
      become: yes
      pip:
        name: docker
